require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to test the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator. If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails. There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.

RSpec.describe "/position_type_weights", type: :request do
  # This should return the minimal set of attributes required to create a valid
  # PositionTypeWeight. As you add validations to PositionTypeWeight, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  # This should return the minimal set of values that should be in the headers
  # in order to pass any filters (e.g. authentication) defined in
  # PositionTypeWeightsController, or in your router and rack
  # middleware. Be sure to keep this updated too.
  let(:valid_headers) {
    {}
  }

  describe "GET /index" do
    it "renders a successful response" do
      PositionTypeWeight.create! valid_attributes
      get position_type_weights_url, headers: valid_headers, as: :json
      expect(response).to be_successful
    end
  end

  describe "GET /show" do
    it "renders a successful response" do
      position_type_weight = PositionTypeWeight.create! valid_attributes
      get position_type_weight_url(position_type_weight), as: :json
      expect(response).to be_successful
    end
  end

  describe "POST /create" do
    context "with valid parameters" do
      it "creates a new PositionTypeWeight" do
        expect {
          post position_type_weights_url,
               params: { position_type_weight: valid_attributes }, headers: valid_headers, as: :json
        }.to change(PositionTypeWeight, :count).by(1)
      end

      it "renders a JSON response with the new position_type_weight" do
        post position_type_weights_url,
             params: { position_type_weight: valid_attributes }, headers: valid_headers, as: :json
        expect(response).to have_http_status(:created)
        expect(response.content_type).to match(a_string_including("application/json"))
      end
    end

    context "with invalid parameters" do
      it "does not create a new PositionTypeWeight" do
        expect {
          post position_type_weights_url,
               params: { position_type_weight: invalid_attributes }, as: :json
        }.to change(PositionTypeWeight, :count).by(0)
      end

      it "renders a JSON response with errors for the new position_type_weight" do
        post position_type_weights_url,
             params: { position_type_weight: invalid_attributes }, headers: valid_headers, as: :json
        expect(response).to have_http_status(:unprocessable_entity)
        expect(response.content_type).to match(a_string_including("application/json"))
      end
    end
  end

  describe "PATCH /update" do
    context "with valid parameters" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested position_type_weight" do
        position_type_weight = PositionTypeWeight.create! valid_attributes
        patch position_type_weight_url(position_type_weight),
              params: { position_type_weight: new_attributes }, headers: valid_headers, as: :json
        position_type_weight.reload
        skip("Add assertions for updated state")
      end

      it "renders a JSON response with the position_type_weight" do
        position_type_weight = PositionTypeWeight.create! valid_attributes
        patch position_type_weight_url(position_type_weight),
              params: { position_type_weight: new_attributes }, headers: valid_headers, as: :json
        expect(response).to have_http_status(:ok)
        expect(response.content_type).to match(a_string_including("application/json"))
      end
    end

    context "with invalid parameters" do
      it "renders a JSON response with errors for the position_type_weight" do
        position_type_weight = PositionTypeWeight.create! valid_attributes
        patch position_type_weight_url(position_type_weight),
              params: { position_type_weight: invalid_attributes }, headers: valid_headers, as: :json
        expect(response).to have_http_status(:unprocessable_entity)
        expect(response.content_type).to match(a_string_including("application/json"))
      end
    end
  end

  describe "DELETE /destroy" do
    it "destroys the requested position_type_weight" do
      position_type_weight = PositionTypeWeight.create! valid_attributes
      expect {
        delete position_type_weight_url(position_type_weight), headers: valid_headers, as: :json
      }.to change(PositionTypeWeight, :count).by(-1)
    end
  end
end
